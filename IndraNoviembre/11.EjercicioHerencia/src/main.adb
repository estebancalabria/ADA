procedure Main is

begin
   --Crear una clase abstracta figura (sin atributos)
   --Crear el metodo abstracto calcular_area
   --Crear la sublase rectangulo(Base,Altura) que herede de Figura
   --Sobrescribir (overriding) el metodo calcular_are
   null;
end Main;

--  with tagged record
--     null
--   end record;
